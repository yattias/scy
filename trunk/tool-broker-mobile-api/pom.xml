<?xml version="1.0" encoding="ISO-8859-1"?>
<project
		xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/maven-v4_0_0.xsd"
		xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
		xmlns="http://maven.apache.org/POM/4.0.0">
	<!-- @version $Revision: 98 $ ($Author: vlads $) $Date: 2007-04-20 20:05:31 -0400 (Fri, 20 Apr 2007) $ -->
	<modelVersion>4.0.0</modelVersion>

	<groupId>eu.scy</groupId>
	<artifactId>tool-broker-mobile-api</artifactId>
	<version>1.0-SNAPSHOT</version>
	<packaging>jar</packaging>

	<name>tool-broker-mobile-api</name>

	<organization>
		<name>SCY</name>
	</organization>

	<repositories>
		<repository>
			<id>pyx4me-web-release</id>
			<url>http://www.pyx4me.com/maven2-release</url>
			<snapshots>
				<enabled>false</enabled>
			</snapshots>
			<releases>
				<enabled>true</enabled>
			</releases>
		</repository>
	</repositories>

	<pluginRepositories>
		<pluginRepository>
			<id>pyx4me-web</id>
			<url>http://www.pyx4me.com/maven2</url>
		</pluginRepository>
	</pluginRepositories>

	<properties>
		<me2Version>2.0.1</me2Version>
		<!-- microemulator version -->

		<wtk.home>${env.WTK_HOME}</wtk.home>

		<j2me.midlet.vendor>${project.organization.name}</j2me.midlet.vendor>
		<j2me.midlet.name>${project.name}</j2me.midlet.name>
		<j2me.midlet.version>1.0-SNAPSHOT</j2me.midlet.version>
		<j2me.midlet.configuration>CLDC-1.1</j2me.midlet.configuration>
		<j2me.midlet.profile>MIDP-2.0</j2me.midlet.profile>

		<!--
				<wtk.bluetooth.enabled>true</wtk.bluetooth.enabled>
				<wtk.wma.enabled>true</wtk.wma.enabled>
				<wtk.wma.version>2.0</wtk.wma.version>
				<wtk.locationservices.enabled>true</wtk.locationservices.enabled>
				<wtk.optionalpda.enabled>true</wtk.optionalpda.enabled>
				-->

	</properties>

	<dependencies>

		<dependency>
			<groupId>org.microemu</groupId>
			<artifactId>microemulator</artifactId>
			<version>${me2Version}</version>
			<scope>provided</scope>
		</dependency>

		<!-- Uncomment for wma
				<dependency>
					<groupId>org.microemu</groupId>
					<artifactId>microemu-jsr-120</artifactId>
					<version>${me2Version}</version>
					<scope>provided</scope>
				</dependency>
				-->

		<!-- Uncomment for JSR-82
				<dependency>
					<groupId>net.sf.bluecove</groupId>
					<artifactId>bluecove</artifactId>
					<version>1.2.2</version>
					<scope>provided</scope>
				</dependency>
				-->

		<!-- Use system scope for jars from WTK
				<dependency>
					<groupId>come.sun.wtk</groupId>
					<artifactId>jsr082</artifactId>
					<version>2.2</version>
					<scope>system</scope>
					<systemPath>${WTK_HOME}/lib/jsr082.jar</systemPath>
				</dependency>
				-->

	</dependencies>

	<build>
		<defaultGoal>install</defaultGoal>

		<plugins>

			<plugin>
				<artifactId>maven-compiler-plugin</artifactId>
				<configuration>
					<source>1.3</source>
					<target>1.3</target>
				</configuration>
			</plugin>

			<plugin>
				<groupId>com.pyx4me</groupId>
				<artifactId>j2me-maven-plugin</artifactId>
				<executions>
					<execution>
						<goals>
							<goal>package</goal>
						</goals>
					</execution>
				</executions>
				<configuration>
					<wtkHome>${env.WTK_HOME}</wtkHome>
					<proguardInclude>proguard.conf</proguardInclude>
					<proguard>false</proguard>
					<obfuscate>true</obfuscate>
				</configuration>
			</plugin>

			<!-- install jar in project site -->
			<plugin>
				<artifactId>maven-antrun-plugin</artifactId>
				<executions>
					<execution>
						<phase>site</phase>
						<goals>
							<goal>run</goal>
						</goals>
						<configuration>
							<tasks>
								<copy overwrite="true"
								      file="${project.build.directory}/${project.build.finalName}-me.jar"
								      tofile="${project.build.directory}/site/tool-broker-mobile-api.jar"/>
								<copy overwrite="true"
								      file="${project.build.directory}/${project.build.finalName}-me.jad"
								      tofile="${project.build.directory}/site/tool-broker-mobile-api.jad"/>
								<replace value="tool-broker-mobile-api.jar" token="${project.build.finalName}-me.jar"
								         dir="${project.build.directory}/site">
									<include name="tool-broker-mobile-api.jad"></include>
								</replace>
							</tasks>
						</configuration>
					</execution>
				</executions>
			</plugin>

			<plugin>
				<groupId>com.pyx4me</groupId>
				<artifactId>gammu-maven-plugin</artifactId>
				<configuration>
					<gammurc>${env.HOME}/gammurc</gammurc>
				</configuration>
			</plugin>

		</plugins>
	</build>

</project>

