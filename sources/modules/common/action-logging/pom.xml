<project xmlns="http://maven.apache.org/POM/4.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
	xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/maven-v4_0_0.xsd">
	<modelVersion>4.0.0</modelVersion>
	<artifactId>action-logging</artifactId>
	<name>action-logging</name>
	<packaging>jar</packaging>
	
 	<parent>
		<artifactId>parent</artifactId>
		<groupId>eu.scy.common</groupId>
		<version>9.5.1-SNAPSHOT</version>
	</parent> 	
	
	<dependencies>
		<dependency>
			<groupId>${pom.groupId}</groupId>
			<artifactId>action-logging-api</artifactId>
			<version>${pom.version}</version>
			<scope>compile</scope>
		</dependency>
        <dependency>
            <groupId>${pom.groupId}</groupId>
            <artifactId>scy-model-impl</artifactId>
            <version>${pom.version}</version>
        </dependency>
		<dependency>
			<groupId>info.collide</groupId>
			<artifactId>sqlspaces-client</artifactId>
			<version>3.1.0-SNAPSHOT</version>
			<type>jar</type>
			<scope>compile</scope>
		</dependency>
        <dependency>
            <groupId>org.springframework</groupId>
            <artifactId>spring</artifactId>
            <version>${spring-release-version}</version>
        </dependency>

        <dependency>
            <groupId>org.springframework</groupId>
            <artifactId>spring-aspects</artifactId>
            <version>${spring-release-version}</version>
        </dependency>

        <dependency>
            <groupId>org.springframework</groupId>
            <artifactId>spring-test</artifactId>
            <version>${spring-release-version}</version>
        </dependency>

        <dependency>
            <groupId>mysql</groupId>
            <artifactId>mysql-connector-java</artifactId>
            <version>5.0.5</version>
            <scope>test</scope>
        </dependency>

	</dependencies>

    <build>
        <filters>
            <filter>${filter.test.file}</filter>
        </filters>
        <testResources>
            <testResource>
                <directory>src/test/java</directory>
                <filtering>true</filtering>
            </testResource>
        </testResources>
    </build>

    <properties>
        <filter.test.file>src/test/resources/filter.properties</filter.test.file>
    </properties>
    
	
</project>